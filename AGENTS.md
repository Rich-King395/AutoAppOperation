# AppCollector Agent Notes

- Keep project code under `src/appcollector`.
- Keep generated run metadata under `logs/runs`.
- Keep raw collection output under `data/raw` and processed output under `data/processed`.
- Prefer small, testable modules for driver setup, flow logic, and experiment orchestration.
